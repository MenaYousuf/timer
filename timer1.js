// const diffTimer = function(time) {
//   let times = []
//   for (let i = 0; i < time.length; i++) {
//     if (time[i] === 3 || 5 || 9 || 10 || 15)
//       return ("beep");
//   };
//   }
 


const diffTimes = function(num){
let delay = 1000

for (let i = 0; i < num.length; i++){
  if (num = num.length[i]);{
    return num[i];
  setTimeout(() => {
    process.stdout.write('\x07');
  }, delay);
}
}console.log(diffTimes())
}

